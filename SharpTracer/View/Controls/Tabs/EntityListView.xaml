<UserControl x:Class="SharpTracer.View.Controls.GeometryListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SharpTracer.View.Controls"
		xmlns:pc  ="clr-namespace:SharpEngine.Engine.Graphics"
        mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="600">
	<UserControl.Resources>
		<DataTemplate DataType="pc:Entity">
			<local:GeometryView/>
		</DataTemplate>
	</UserControl.Resources>
	<DockPanel>
		<Label Content="{Binding Title}"  DockPanel.Dock="Top"/>
		<local:EntityView DataContext="{Binding SelectedEntity}" DockPanel.Dock="Top"/>
		<DataGrid ItemsSource="{Binding Entitys}" SelectedItem="{Binding SelectedEntity}" AutoGenerateColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False"
				   CanUserResizeColumns="False" CanUserResizeRows="False" CanUserAddRows="False" SelectionMode="Single">
			<DataGrid.Columns>
			<DataGridTemplateColumn Width="1*">
				<DataGridTemplateColumn.CellTemplate>
					<DataTemplate>
						<local:GeometryView/>
					</DataTemplate>
				</DataGridTemplateColumn.CellTemplate>
			</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>
    </DockPanel>
</UserControl>
